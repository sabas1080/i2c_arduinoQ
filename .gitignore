# Binary kernel modules — shipped via GitHub Releases, never in git.
*.ko

# Backups generated by patch / overlay tools.
*.dtb.backup
*.dtb.bak

# Local environment files.
.env
.env.local

# Claude Code local state.
.claude/

# Measurement notes from development — kept locally, not republished.
notes/

# Internal planning docs — not shipped in public release.
docs/superpowers/
